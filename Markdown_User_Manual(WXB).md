# Markdown User Manual
---
> MD 文件是基于 Markdown 语法的纯文本文件，核心优势是「简单易写、跨平台兼容、格式统一」，无需复杂排版，用纯文本符号就能实现标题、列表、表格等格式
---
## MD 核心编辑语法:
> 1. 标题（层级清晰，用 # 表示）
用 # 开头，后跟空格，# 数量对应标题层级（1-6 级），建议 1 级标题只留 1 个（文档总标题）。
markdown
# 1 级标题（最大，如文档标题）
## 2 级标题（章节标题）
### 3 级标题（子章节）
#### 4 级标题
##### 5 级标题
###### 6 级标题（最小）


2. 段落与换行
段落：直接输入文字，段落之间空 1 行即可分隔（无需缩进）。
强制换行：在文字末尾加 2 个空格 + 回车（或用 <br> 标签）。
markdown
这是第一个段落，直接输入文字即可。
（这里空 1 行）
这是第二个段落，末尾加 2 个空格  （回车后强制换行）
这是换行后的内容。
效果：这是第一个段落，直接输入文字即可。
这是第二个段落，末尾加 2 个空格这是换行后的内容。
3. 强调（粗体、斜体、删除线）
用特定符号包裹文字，实现强调效果：
markdown
- 斜体：用 1 个星号（*）或下划线（_）包裹 → *斜体文本* / _斜体文本_
- 粗体：用 2 个星号（**）或下划线（__）包裹 → **粗体文本** / __粗体文本__
- 粗斜体：用 3 个星号（***）包裹 → ***粗斜体文本***
- 删除线：用 2 个波浪线（~~）包裹 → ~~删除线文本~~
效果：
斜体：斜体文本 / 斜体文本
粗体：粗体文本 / 粗体文本
粗斜体：粗斜体文本
删除线：删除线文本
4. 列表（有序、无序、任务列表）
（1）无序列表（用 - * + 均可，后跟空格）
markdown
- 列表项 1
- 列表项 2
  - 子列表项 2.1（缩进 2 个空格或 1 个 Tab）
  - 子列表项 2.2
* 列表项 3（用 * 替代 -，效果一致）
效果：
列表项 1
列表项 2
子列表项 2.1
子列表项 2.2
列表项 3
（2）有序列表（用「数字 + . + 空格」表示）
markdown
1. 第一步：安装编辑器
2. 第二步：学习基础语法
   1. 子步骤 2.1：标题语法
   2. 子步骤 2.2：列表语法
3. 第三步：实战编辑
效果：
第一步：安装编辑器
第二步：学习基础语法
子步骤 2.1：标题语法
子步骤 2.2：列表语法
第三步：实战编辑
（3）任务列表（用 - [ ] 表示未完成，- [x] 表示已完成）
markdown
- [x] 学习 MD 核心语法
- [x] 选择合适的编辑器
- [ ] 编写第一篇 MD 笔记
- [ ] 导出为 PDF 格式
效果：
 学习 MD 核心语法
 选择合适的编辑器
 编写第一篇 MD 笔记
 导出为 PDF 格式
5. 链接与图片
（1）链接（跳转外部页面或内部锚点）
基础格式：[链接文本](链接地址 "可选说明，hover时显示")
内部锚点：跳转到文档内标题（需标题存在），格式：[跳转到1级标题](#1-级标题)（注意：标题中的空格用 - 替代，大写转小写）
markdown
- 外部链接：[GitHub官网](https://github.com "全球最大代码托管平台")
- 内部锚点：[跳转到「列表」章节](#4-列表有序无序任务列表)
效果：
外部链接：GitHub 官网
内部锚点：跳转到「列表」章节
（2）图片（插入本地或网络图片）
基础格式：![图片加载失败时的提示文本](图片路径/URL "可选说明")
本地图片：直接拖入编辑器（部分编辑器支持自动生成路径），或手动输入相对路径（如 ./images/photo.jpg）
网络图片：粘贴图片 URL 即可
markdown
- 网络图片：![跑步示意图](https://example.com/run.jpg "跑步姿势参考")
- 本地图片：![我的笔记截图](./screenshots/note.png)
效果：
网络图片：

image
（若 URL 无效，显示「跑步示意图」）
本地图片：需确保路径正确，否则无法显示
6. 引用（用 > 表示，适合引用他人观点、备注）
markdown
> 引用文本：Markdown 是一种轻量级标记语言，由 John Gruber 设计。
> 嵌套引用：
>> 这是二级引用，在一级引用内加 > 即可。
> 引用后接普通文本（空 1 行即可）
效果：
引用文本：Markdown 是一种轻量级标记语言，由 John Gruber 设计。嵌套引用：
这是二级引用，在一级引用内加 > 即可。引用后接普通文本（空 1 行即可）
7. 表格（用 | 分隔列，- 分隔表头与内容）
基础格式：| 表头1 | 表头2 | 表头3 | + 换行 + | --- | --- | --- |（--- 数量不影响，可加 : 控制对齐）
对齐方式：: 靠左 → 左对齐，: 靠右 → 右对齐，: 两边 → 居中对齐
markdown
| 姓名 | 年龄 | 职业       |
| :--- | :--- | :--------- |
| 张三 | 25   | 数据分析师 |
| 李四 | 30   | 软件工程师 |
| 王五 | 28   | 产品经理   |

# 对齐示例（左对齐、居中、右对齐）
| 左对齐 | 居中对齐 | 右对齐 |
| :----- | :------: | -----: |
| 文本1  |   文本2  |  文本3 |
效果：
姓名	年龄	职业
张三	25	数据分析师
李四	30	软件工程师
王五	28	产品经理
左对齐	居中对齐	右对齐
文本 1	文本 2	文本 3
8. 分割线（用 --- *** ___ 均可，单独成行）
markdown
这是分割线上方的内容
---
这是分割线下方的内容
***
另一条分割线（效果一致）
效果：这是分割线上方的内容
这是分割线下方的内容
另一条分割线（效果一致）
9. 代码块与行内代码（之前重点讲过，简要回顾）
行内代码：代码内容（单个反引号包裹）
代码块：语言 代码内容 （三反引号包裹，支持语法高亮）
markdown
- 行内代码：`print("Hello MD")`
- 代码块：
```python
def add(a, b):
    return a + b
plaintext

效果：
- 行内代码：`print("Hello MD")`
- 代码块：
```python
def add(a, b):
    return a + b
三、MD 进阶编辑技巧（提升效率与颜值）
1. 格式优化技巧
空行分隔：标题、列表、引用、表格之间空 1 行，文档更整洁（避免格式错乱）。
缩进规范：子列表、代码块在列表中时，缩进 2 个空格或 1 个 Tab，保持层级一致。
避免特殊字符冲突：若文本中包含 # * | 等语法符号，在符号前加反斜杠（\）转义，如 \# 这不是标题。
2. 插件扩展（以 VS Code 为例）
实时预览：安装「Markdown Preview Enhanced」，按 Ctrl+Shift+V 打开侧边预览，支持同步滚动。
导出功能：安装「Markdown PDF」，右键 MD 文件可导出为 PDF/HTML/PNG 格式（适合分享文档）。
数学公式：安装「Markdown+Math」，支持 LaTeX 语法，如 $E=mc^2$（行内公式）、$$\sum_{i=1}^n i = \frac{n(n+1)}{2}$$（块级公式）。
流程图 / 时序图：支持 Mermaid 语法，示例：
markdown
```mermaid
graph TD
  A[开始] --> B[学习 MD 语法]
  B --> C{掌握核心语法？}
  C -->|是| D[编写 MD 文档]
  C -->|否| B
  D --> E[导出/分享]
  E --> F[结束]
plaintext
效果：生成流程图（需编辑器支持 Mermaid）。
3. 批量编辑与格式转换
批量修改：用 VS Code 的「查找替换」（Ctrl+H），支持正则表达式（如批量替换标题层级、修改链接前缀）。
格式转换：
在线工具：「Convertio」「Zamzar」可将 MD 转为 Word/PDF/HTML。
本地工具：Typora 直接导出为 PDF/HTML/EPUB；Pandoc（命令行工具）支持 MD 与多种格式互转（如 pandoc input.md -o output.docx）。
4. 云同步与协作
个人同步：将 MD 文件存放在 Dropbox、OneDrive、iCloud 文件夹，实现多设备同步。
团队协作：用 GitHub/GitLab 托管 MD 文档（支持版本控制），或用 Notion、语雀直接协作编辑。
四、实战场景：MD 文件的常见用途
结合你的「计算机学习者」和「跑步爱好者」双重身份，分享 2 个高频场景：
1. 技术学习笔记（如 Python / 数据库学习）
markdown
# Python 基础语法学习笔记（2024.10.01）
## 一、变量与数据类型
### 1. 变量命名规则
- 只能包含字母、数字、下划线，不能以数字开头
- 区分大小写（`name` 和 `Name` 是不同变量）
- 不能使用 Python 关键字（如 `if` `for`）

### 2. 常用数据类型
| 类型   | 示例          | 说明               |
| :----- | :------------ | :----------------- |
| 整数   | `a = 10`      | 无小数部分         |
| 字符串 | `b = "hello"` | 用引号包裹         |
| 列表   | `c = [1,2,3]` | 有序、可修改的集合 |

## 二、代码示例：计算两数之和
```python
def calculate_sum(a: int, b: int) -> int:
    """计算两数之和（文档字符串）"""
    return a + b

# 测试代码
result = calculate_sum(5, 3)
print(f"5 + 3 = {result}")  # 输出：5 + 3 = 8
plaintext

### 2. 跑步训练日志
```markdown
# 2024 跑步训练日志
## 10 月训练计划
| 日期       | 距离（km） | 配速（min/km） | 心率（bpm） | 训练类型       | 备注               |
| :--------- | :--------- | :------------- | :---------- | :------------- | :----------------- |
| 2024.10.01 | 5          | 5:30           | 145         | 轻松跑         | 晨跑，天气凉爽     |
| 2024.10.03 | 8          | 5:10           | 152         | 节奏跑         | 后半程略有疲惫     |
| 2024.10.05 | 10         | 4:50           | 160         | 长距离慢跑     | 补充电解质，无不适 |

## 训练心得
> 节奏跑时需注意呼吸节奏，两步一吸、两步一呼，避免心率过高导致疲劳。
> 长距离跑前需热身 10 分钟，跑后拉伸小腿、大腿肌肉，预防抽筋。

### 待优化点
- [x] 提升配速稳定性（避免前快后慢）
- [ ] 增加核心力量训练（每周 2 次）
- [ ] 学习正确的跑姿（减少膝盖压力）
五、常见问题与解决方案
1. 格式不生效？
检查语法符号：是否用英文符号（如中文 # * 无效）。
检查空格：标题、列表、链接等语法后需加 1 个空格（如 #标题 无效，# 标题 有效）。
检查编辑器支持：部分语法（如任务列表、流程图）需编辑器支持（如记事本不支持实时预览）。
2. 本地图片无法显示？
路径错误：确保图片路径与 MD 文件路径对应（如 MD 文件在 note 文件夹，图片在 note/images，路径写 ./images/photo.jpg）。
绝对路径问题：尽量用相对路径（跨设备 / 平台兼容），避免绝对路径（如 C:/Users/xxx/photo.jpg）。
3. 导出 PDF 格式错乱？
编辑器选择：用 Typora 或 VS Code 插件导出（兼容性更好）。
检查格式：避免表格过宽、代码块过长（可拆分表格或代码块）。
字体设置：导出前在编辑器中设置字体大小、行间距，确保排版整齐。
4. 协作时格式不一致？
统一编辑器：团队约定使用相同编辑器（如 VS Code + 统一插件）。
遵循规范：制定简单的 MD 编写规范（如标题层级、列表缩进、表格对齐方式）。
