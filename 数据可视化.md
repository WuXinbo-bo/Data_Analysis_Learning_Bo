# 🛠️ sns + plt 绘图落地全流程（指南）
---

编写：WXB （内容基于Gemini 3.0 Pro 总结）

---
***
# 内容概要

# 第一步：全局初始化设置 (Global Setup)
在绘图开始前，先设定好“基调”，确保所有图表的风格统一，并解决常见问题（如中文乱码）。

[sns和plt调整绘画风格总结](#Seaborn和Matplotlib调整绘画风格总结)



>导入库：
```
Python

import matplotlib.pyplot as plt
import seaborn as sns
```
>设置主题 (Seaborn)： 使用 sns 快速定下美观的背景和色调。
```
Python

sns.set_theme(style="whitegrid")  # 可选: darkgrid, white, ticks
```
>解决中文显示 (Matplotlib)： (如果是中文环境必做)
```
Python

plt.rcParams['font.sans-serif'] = ['SimHei'] # 或 Arial Unicode MS
plt.rcParams['axes.unicode_minus'] = False   # 解决负号显示问题
```
# 第二步：创建画布容器 (Canvas & Axes)
>不要直接用 plt.plot()，而是使用 面向对象 的方式创建 figure (画布) 和 ax (子图/坐标轴)。这是专业绘图的关键，方便后续精确控制。

[创建画布容器的详细说明](#创建画布容器的详细说明核心函数)
```
Python

# 创建一个宽10, 高6的画布，fig是整张图，ax是具体的绘图区域
fig, ax = plt.subplots(figsize=(10, 6))
```

# 第三步：绘制核心特征图 (Core Plotting via SNS)
>使用 Seaborn 强大的统计绘图功能来生成图表，并将图画在指定的 ax 上。
 
[绘制基本图形代码指南](#绘制基本图形代码指南)
>关键点： 几乎所有 sns 函数都有 ax= 参数。

>语法： sns.plot_name(data=df, x='...', y='...', ax=ax)
```
Python

# 例如：绘制散点图，并指定画在上面创建的 ax 上
sns.scatterplot(data=df, x='feature_A', y='target', hue='category', ax=ax)
```
# 第四步：精细化修饰 (Refinement via PLT/AX)
>图画好了，现在用 Matplotlib 的功能对 ax 进行“装修”。这一步决定了图表的可读性。

>标题与标签：
```
Python

ax.set_title("特征A与目标值的关系", fontsize=16)
ax.set_xlabel("特征A (单位)")
ax.set_ylabel("目标值")
```
>坐标轴控制：
```
Python

ax.set_xlim(0, 100)       # 限制X轴范围
plt.xticks(rotation=45)   # 旋转刻度标签(防重叠)
```
>图例与其他：
```
Python

ax.legend(loc='upper right') # 调整图例位置
ax.grid(True, alpha=0.3)     # 调整网格透明度
```

# 第五步：布局调整与输出 (Layout & Output)
>最后一步是整理布局，避免文字被遮挡，并保存或显示。

>自动紧凑布局 (强烈推荐)：
```
Python

plt.tight_layout() # 自动调整子图间距，防止标签被切掉
```
>保存或显示：
```
Python

# 保存为高清图片 (用于报告/PPT)
plt.savefig('output_chart.png', dpi=300, bbox_inches='tight')

# 屏幕显示
plt.show()
```
---

## 📝 流程速记口诀
先 Set：设置风格 sns.set_theme。

造 Ax：创建对象 fig, ax = plt.subplots。

画 SNS：绘图入位 sns.xxx(..., ax=ax)。

修细节：标题标签 ax.set_title/label。

存图表：紧凑保存 tight_layout + savefig。

***
***
# 指南目录：

1.[sns和plt调整绘画风格总结](#Seaborn和Matplotlib调整绘画风格总结)

2.[创建画布容器的详细说明](#创建画布容器的详细说明核心函数)

3.[绘制基本图形代码指南](#绘制基本图形代码指南)

# 以下内容为详细指南
---
---

# Seaborn和Matplotlib调整绘画风格总结

#### 1. Seaborn 风格调整

- **设置 Seaborn 风格**  
  使用 `sns.set()` 方法调整图形的背景、网格和其他元素的风格。常见的风格选项包括：
  - `darkgrid`：黑色背景网格（默认）
  - `whitegrid`：白色背景网格
  - `dark`：黑色背景无网格
  - `white`：白色背景无网格
  - `ticks`：白色背景并显示刻度线  
  **代码简述**：`sns.set(style="whitegrid")` 设置白色背景带网格。

- **调色板**  
  通过 `sns.set_palette()` 设置图形的颜色主题。Seaborn 提供了多种预设的调色板，如 `muted`、`deep`、`pastel` 等，或者你可以自定义调色板。  
  **代码简述**：`sns.set_palette("muted")` 设置为 muted 调色板，或 `sns.set_palette(["#FF6347", "#4682B4"])` 自定义调色板。

- **字体和文本属性**  
  `sns.set_context()` 用于调整图形的整体文本大小、线条粗细等元素。可以选择不同的上下文，如 `paper`、`notebook`、`talk` 和 `poster`，来调整图形的比例。  
  **代码简述**：`sns.set_context("notebook")` 设置文本和元素的大小适合笔记本的比例。

- **网格线调整**  
  通过 `sns.set(style="ticks")` 等选项调整网格线的显示，或者使用 `sns.set(style="whitegrid")` 使背景具有网格。  
  **代码简述**：`sns.set(style="ticks")` 显示带刻度的背景，`sns.set(style="whitegrid")` 显示白色背景网格。

#### 2. Matplotlib 风格调整

- **设置预定义风格**  
  Matplotlib 提供了多种预定义的风格，如 `ggplot`、`seaborn`、`bmh` 等，使用 `plt.style.use()` 选择合适的样式。  
  **代码简述**：`plt.style.use('ggplot')` 应用 `ggplot` 风格。

- **全局字体和线条样式**  
  使用 `plt.rcParams` 设置全局的字体、颜色、线条样式等。例如，可以改变字体类型、线条的粗细和颜色等。  
  **代码简述**：`plt.rcParams['font.family'] = 'Arial'` 设置全局字体为 Arial。

- **坐标轴样式**  
  Matplotlib 允许定制坐标轴标签的字体、字体大小，调整坐标轴范围以及坐标轴刻度的显示样式。  
  **代码简述**：`plt.xlabel('X轴', fontsize=14)` 设置 X 轴标签字体大小为 14，`plt.xlim(0, 10)` 设置 X 轴范围为 0 到 10。

- **网格线调整**  
  可以启用或自定义网格线的显示，调整网格线的颜色、样式和宽度，以适应图形的美观需求。  
  **代码简述**：`plt.grid(True, color='gray', linestyle='-', linewidth=0.5)` 设置灰色实线网格，线宽为 0.5。

#### 3. 结合使用 Seaborn 和 Matplotlib 风格设置

- 由于 Seaborn 是基于 Matplotlib 构建的，因此它继承了 Matplotlib 的许多设置。可以通过 Seaborn 设置图形风格，同时结合 Matplotlib 的细节调整，例如坐标轴、字体、线条等。  
  **代码简述**：`sns.set(style="whitegrid")` 设置 Seaborn 风格，接着使用 `plt.title("Title")` 调整标题。

- Seaborn 通过 `sns.set()` 等方法设置全局风格和配色方案，而 Matplotlib 可以进一步细化图形的具体元素，如标题、坐标轴、字体、线条样式等。  
  **代码简述**：`sns.set(style="darkgrid")` 设置网格样式，之后用 `plt.xlabel('X Label')` 调整 X 轴标签。

通过合理使用 Seaborn 和 Matplotlib 的风格设置，可以快速创建既美观又符合需求的图形。
```
===关于plt与sns的介绍：===
1. 库的背景和设计目的:
matplotlib.pyplot (plt)：
plt 是 matplotlib 库的一部分，提供了一套底层的绘图库，用于创建各种类型的图表。
适用于用户需要精细控制图表的各个细节时。
更加灵活，但可能需要编写更多的代码来实现复杂的图形。
seaborn (sns)：
seaborn 是基于 matplotlib 的高级绘图库，旨在使数据可视化更加简洁且具有美观的默认样式。
它主要聚焦于统计数据的可视化，提供了很多内建的函数来处理复杂的图表（如箱线图、热图、回归图等）。
它对数据帧（如 pandas DataFrame）有更好的支持。

2. 使用难度与简洁性：
matplotlib.pyplot：
需要手动控制图表的各个部分，如坐标轴、标签、标题等。
代码量较大，灵活性强。
可以精细调整所有的细节，但不如 seaborn 简洁。
seaborn：
提供了更简洁和直观的接口来绘制复杂的统计图表。
seaborn 默认样式更美观，减少了需要手动设置的部分。
很多功能只需要一行代码即可完成，如绘制箱线图、直方图、散点图等。

3. 绘图功能：
matplotlib.pyplot：
提供最基本的绘图功能，支持各种类型的图表，如线图、散点图、条形图、直方图等。
适合需要高度自定义的绘图需求。
seaborn：
主要用于统计图表的绘制，如热图、箱型图、回归图、分类图、分布图等。
对于大多数常见的统计可视化，seaborn 提供了高度封装好的接口。
例如，seaborn 可以通过简单的命令轻松绘制分组柱状图、成对图、联合分布图等。
```
### 案例

#### `seaborn` 和 `matplotlib.pyplot` 的结合使用

> 1. **概述**
- **`seaborn`** 是基于 `matplotlib` 的高级绘图库，提供了更简洁且美观的接口，专注于统计数据可视化。
- **`matplotlib.pyplot`** 提供底层的绘图控制，适用于需要精细定制的图表。

这两个库可以一起使用，利用 `seaborn` 简洁的接口绘制统计图表，再通过 `matplotlib` 进行细节调整和自定义。

> 2. **如何同时使用 `seaborn` 和 `matplotlib`**

 示例 1: 使用 `seaborn` 绘制图表并用 `matplotlib` 调整外观

```python
import seaborn as sns
import matplotlib.pyplot as plt

# 载入示例数据集
tips = sns.load_dataset("tips")

# 使用 seaborn 绘制箱线图
sns.boxplot(x="day", y="total_bill", data=tips)

# 用 matplotlib 进行一些额外的调整
plt.title("Total Bill by Day")  # 设置标题
plt.xlabel("Day of the Week")  # 设置 x 轴标签
plt.ylabel("Total Bill ($)")  # 设置 y 轴标签

# 显示图表
plt.show()
```
import seaborn as sns
import matplotlib.pyplot as plt

示例 2: 通过 seaborn 创建多个子图，并通过 matplotlib 设置图表大小
```
# 载入数据
tips = sns.load_dataset("tips")

# 创建一个 1x2 的子图布局
fig, axes = plt.subplots(1, 2, figsize=(12, 6))

# 使用 seaborn 绘制图表
sns.boxplot(x="day", y="total_bill", data=tips, ax=axes[0])
sns.scatterplot(x="total_bill", y="tip", data=tips, ax=axes[1])

# 设置每个子图的标题
axes[0].set_title("Total Bill by Day")
axes[1].set_title("Total Bill vs Tip")

# 显示图表
plt.tight_layout()  # 调整子图之间的间距
plt.show()
```
***
***
# 创建画布容器的详细说明核心函数

在 Matplotlib 和 Seaborn 绘图流程中，创建画布容器 (Canvas & Axes) 是第二步，也是最重要的一步。它标志着从“状态机”模式转向 面向对象 (Object-Oriented, OO) 绘图模式，从而获得对图表的精确控制。

## 一、核心原理：Figure 与 Axes 的关系

在 Matplotlib 的 OO 范式中，有两个核心概念：

| 对象        | 对应现实  | 职责/功能  | 核心方法                |
|-------------|-----------|-----------|-------------------------|
| **Figure (fig)** | 画布/画廊  | 顶层容器，代表整个图像窗口。控制全局属性（如尺寸、分辨率），并包含所有 Axes 对象。 | `plt.figure()`, `fig.savefig()` |
| **Axes (ax/axes)** | 坐标轴/子图  | 实际的绘图区域，包含 X/Y 轴、刻度线、数据点等。 | `ax.plot()`, `ax.set_title()`, `sns.xxx(ax=ax)` |

## 二、基本操作：创建单个图表

### 函数： plt.subplots()

这是最常用且最推荐的方式，它能一步到位地创建 Figure 和 Axes 对象。

### 1. 代码结构

```python
import matplotlib.pyplot as plt

# 1. 一步创建 Figure 和 Axes
# figsize=(宽度, 高度)，单位通常为英寸
fig, ax = plt.subplots(figsize=(10, 6)) 

# 2. 后续所有的绘图和定制都针对 ax 进行
# sns.scatterplot(..., ax=ax)
# ax.set_title("图表标题")
```

***
***

# 绘制基本图形代码指南

在数据可视化中，第二步“绘制基本图形”是将数据转化为视觉图形的关键步骤。常用的基本图形包括各种类型的图表，可以帮助我们清晰地展示数据的分布、关系、趋势等信息。以下是使用 Seaborn 和 Matplotlib 常见的基本图形及其绘制方法：

## 1. 散点图 (Scatter Plot)

散点图是用来显示两个变量之间关系的基本图形。它通过点的位置来展示数据点的分布。

- **Seaborn**：使用 `sns.scatterplot()` 绘制散点图。
- **Matplotlib**：使用 `plt.scatter()` 绘制散点图。

#### 常见用途
- 显示两列数值数据之间的关系
- 判断数据的线性或非线性关系

#### 代码简述：
- **Seaborn**：`sns.scatterplot(data=df, x='x_column', y='y_column')`
- **Matplotlib**：`plt.scatter(df['x_column'], df['y_column'])`

## 2. 折线图 (Line Plot)

折线图用于显示数据点之间的趋势，常用来展示随时间变化的数据。

- **Seaborn**：使用 `sns.lineplot()` 绘制折线图。
- **Matplotlib**：使用 `plt.plot()` 绘制折线图。

#### 常见用途
- 展示时间序列数据
- 展示数据的变化趋势

#### 代码简述：
- **Seaborn**：`sns.lineplot(data=df, x='time_column', y='value_column')`
- **Matplotlib**：`plt.plot(df['time_column'], df['value_column'])`

## 3. 柱状图 (Bar Plot)

柱状图用于展示类别数据的数量或频率，条形的高度或长度表示数据的大小。

- **Seaborn**：使用 `sns.barplot()` 绘制柱状图。
- **Matplotlib**：使用 `plt.bar()` 绘制柱状图。

#### 常见用途
- 比较不同类别之间的数值大小
- 展示分类数据的频率分布

#### 代码简述：
- **Seaborn**：`sns.barplot(x='category_column', y='value_column', data=df)`
- **Matplotlib**：`plt.bar(df['category_column'], df['value_column'])`

## 4. 直方图 (Histogram)

直方图展示数据的分布，通常用于数值数据，展示数据点的频率分布情况。

- **Seaborn**：使用 `sns.histplot()` 绘制直方图。
- **Matplotlib**：使用 `plt.hist()` 绘制直方图。

#### 常见用途
- 显示数据分布情况（如正态分布、偏态分布等）
- 了解数据的集中趋势和分散情况

#### 代码简述：
- **Seaborn**：`sns.histplot(data=df['value_column'], bins=20)`
- **Matplotlib**：`plt.hist(df['value_column'], bins=20)`

## 5. 箱线图 (Box Plot)

箱线图用于展示数据的分布情况，包括中位数、四分位数、最大值、最小值等统计量。

- **Seaborn**：使用 `sns.boxplot()` 绘制箱线图。
- **Matplotlib**：使用 `plt.boxplot()` 绘制箱线图。

#### 常见用途
- 显示数据的分布、离群值、上下四分位数
- 比较不同类别之间的分布差异

#### 代码简述：
- **Seaborn**：`sns.boxplot(x='category_column', y='value_column', data=df)`
- **Matplotlib**：`plt.boxplot(df['value_column'])`

## 6. 热力图 (Heatmap)

热力图用于展示矩阵数据，利用颜色深浅来表示数值大小。

- **Seaborn**：使用 `sns.heatmap()` 绘制热力图。
- **Matplotlib**：使用 `plt.imshow()` 或 `plt.pcolor()` 绘制热力图。

#### 常见用途
- 展示相关性矩阵
- 可视化大规模的数据表格

#### 代码简述：
- **Seaborn**：`sns.heatmap(data=df.corr(), annot=True, cmap='coolwarm')`
- **Matplotlib**：`plt.imshow(matrix_data, cmap='coolwarm')`

## 7. 条形图 (Horizontal Bar Plot)

条形图与柱状图类似，但它是横向的，适用于类别名称较长或需要展示横向比较的情况。

- **Seaborn**：使用 `sns.barplot()`，并设置 `orient='h'`。
- **Matplotlib**：使用 `plt.barh()` 绘制条形图。

#### 常见用途
- 展示类别数据的横向比较

#### 代码简述：
- **Seaborn**：`sns.barplot(x='value_column', y='category_column', data=df, orient='h')`
- **Matplotlib**：`plt.barh(df['category_column'], df['value_column'])`

## 8. 饼图 (Pie Chart)

饼图用于展示每个部分在整体中的占比。

- **Matplotlib**：使用 `plt.pie()` 绘制饼图。

#### 常见用途
- 展示分类数据的比例
- 适用于少数类别的比例展示

#### 代码简述：
- **Matplotlib**：`plt.pie(df['value_column'], labels=df['category_column'], autopct='%1.1f%%')`

## 9. 小提琴图 (Violin Plot)

小提琴图结合了箱线图和核密度估计，展示数据的分布、离群值以及密度。

- **Seaborn**：使用 `sns.violinplot()` 绘制小提琴图。

#### 常见用途
- 比较多个类别的数据分布
- 查看数据的对称性、集中趋势和分布

#### 代码简述：
- **Seaborn**：`sns.violinplot(x='category_column', y='value_column', data=df)`

## 10. 成对关系图 (Pair Plot)

成对关系图用于展示多个变量之间的关系，常用于多维数据的可视化。

- **Seaborn**：使用 `sns.pairplot()` 绘制成对关系图。

#### 常见用途
- 展示多维数据集中的变量间关系
- 观察数据点的分布和趋势

#### 代码简述：
- **Seaborn**：`sns.pairplot(df)`

## 总结

绘制基本图形的关键步骤通常涉及选择适合的图表类型并根据数据的特性调整图形。常用的基本图形包括：
- **散点图**：展示两变量关系
- **折线图**：展示数据趋势
- **柱状图**：比较类别数据
- **直方图**：展示数据分布
- **箱线图**：展示数据分布的统计信息
- **热力图**：可视化矩阵数据
- **饼图**：展示部分与整体的比例
- **小提琴图**：展示分布与密度

