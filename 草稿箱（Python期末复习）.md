# python 期末复习（作业选填）
---
### 作业1️⃣

>1.(单选题，5 分)
下面不属于Python保留字的是:A  
A.type
B.def
C.elif
D.import

```
Python 保留字就是被 Python 语言占用、有固定用途的单词，不能拿来当变量名、函数名用。`
```

>2.(单选题，5 分)
哪个选项给出的保留字不直接用于表示分支结构?D  
A.elif
B.else
C.if
D.in

>3.(单选题，5 分)
以下不属于IPO模型的是:C  
A.Output
B.Input
C.Program
D.Process

```
IPO模型是 input process output，IPO 模型是程序设计中的核心逻辑框架，
>分别对应 Input（输入）、Process（处理）、Output（输出） 三个环节。
```

>4.(单选题，5 分)
下列选项中,不属于Python语言特点的是()A  
A.面向过程
B.开源
C.可移植性
D.简单易学

```
python是一门多范式的编程语（人话：即支持面向对象（OOP），又面向过程）所以不是其语言特点
```

>5.(单选题，5 分)
关于Python语句P=-P , 哪个选项的描述是正确的?A  
A.给P赋值为它的负数
B.P等于它的负数
C.P的绝对值
D.P=0

>6.(单选题，5 分)
以下不是Python语言合法命名的是:A  
A.5MyGod
B.MyGod
C._MyGod_
D.MyGod5

```
python变量命名：
  1. 开头只能是字母或下划线，不能是数字或特殊符号
  2. 后续字符可以是字母、数字、下划线
  3. 不能用Python保留字（如if、def、class）
  4. 区分大小写（如num和Num是两个不同的变量）
```

>7.(单选题，5 分)
以下关于Python语言中“缩进”说法正确的是:C  
A.缩进可以用在任何语句之后,表示语句间的包含关系
B.缩进统一为4个空格
C.缩进在程序中长度统一且强制使用
D.缩进是非强制的,仅为了提高代码可读性  
```
Python语言的缩进只要统一即可,不一定是4个空格(尽管这是惯例)。
```

>8.(单选题，5 分)
在Python中,用于获取用户输入的函数是:C  
A.print()
B.get()
C.input()
D.eval()

>9.(判断题， 5 分)
Python代码的注释只有一种方式,那就是使用#符号。❌  

>10.(判断题， 5 分)
程序的输出方式包括：控制台输出、图形输出、文件输出、网络输出、操作系统内部变量输出等。✅

>11.(判断题， 5 分)
Python中字符串的下标是从1开始的。❌

>12.(判断题， 5 分)
Python使用缩进来体现代码之间的逻辑关系。✅

>13.(判断题， 5 分)
Python是一种跨平台、开源、免费的高级动态编程语言。✅

>14.(判断题， 5 分)
在Windows平台上编写的Python程序无法在Unix平台运行。✅

>15.(判断题， 5 分)
处理（Process）是程序对输入数据进行计算产生输出结果的过程。✅

>16.(填空题， 5 分)
打开IDLE，点击Ctrl+N打开一个新窗口，输入语句并保存，使用快键建（`F5`）即可运行该程序。

>17.(填空题， 5 分)
如果想测试变量的类型,可以使用`type`来实现。

>18.(填空题， 5 分)
切片选取的区间是左闭`开`型,不包含结束位的值。

>19.(填空题， 5 分)
Python3提供了`input`函数从标准输入(如键盘)读入一行文本。

>20.(填空题， 5 分)
字符串是一种表示`文本`数据的类型。
---

### 作业2️⃣

>1.(单选题，3.3 分)
字符串是一个字符序列,给字符串s,以下表示s从右侧向左第三个字符的是:B    
A.s[3]
B.s[-3]
C.s[:-3]
D.s[0:-3]

>2.(单选题，3.3 分)
当需要在字符串中使用特殊字符时,Python使用( )作为转义字符。A  
A.\
B.%
C./
D.#

>3.(单选题，3.3 分)
下列方法中,能够返回某个子串在字符串中出现次数的是( )。B  
A.index
B.count
C.find
D.length

>4.(单选题，3.3 分)
pow(x, 0.5)能够计算x的平方根,计算负数的平方根将产生:D  
A.ValueError错误
B.无输出
C.程序崩溃
D.复数

>5.(单选题，3.3 分)
Plain Text  
for i in range(0,2):    print(i,end=' ') 哪个选项是以上程序的输出结果?A  
A.0 1
B.0 1 2
C.1 2
D.1

>6.(单选题，3.3 分)
下列方法中,能够让所有单词的首字母变成大写的方法是( )。A  
A.title
B.upper
C.ljust
D.capitalize

>7.(单选题，3.3 分)
关于整数类型的4种进制表示,哪个选项的描述是正确的?C   
A.二进制、四进制、十进制、十六进制
B.二进制、四进制、八进制、十进制
C.二进制、八进制、十进制、十六进制
D.二进制、四进制、八进制、十六进制
 
```
python不支持四进制
```

>8.(单选题，3.3 分)
val=pow(2,1000),请用一行代码返回val结果的长度值。A
A.len(str(val))
B.len(pow(2,1000))
C.其他答案均不正确
D.len(val)

>9.(单选题，3.3 分)
Plain Text
k=10000 while k>1:    print(k)    k=k/2 哪个选项给出了上述程序的输出次数?C   
A.14
B.1000
C.15
D.13

>10.(单选题，3.3 分)
以下不是Python数据类型的是:C
A.字符串
B.列表
C.实数
D.整数

>11.(单选题，3.3 分)
哪个选项是Python语言%运算符的含义?A  
A.x与y之商的余数
B.x的y次幂
C.x与y之商
D.x与y的整数商

>12.(单选题，3.3 分)
利用print()格式化输出,哪个选项用于控制浮点数的小数点后两位输出?B  
A.{.2}
B.{:.2f}
C.{.2f}
D.{:.2}

>13.(单选题，3.3 分)
for var in ___:  
    print(var)  
哪个选项不符合上述程序空白处的语法要求?D  
A.(1,2,3)
B.range(0,10)
C."Hello"
D.{1;2;3;4;5}

>14.(单选题，3.3 分)
下面关于time库引用不正确的是?A  
A.from * import time
B.from time import strftime
C.from time import *
D.import time

>15.(单选题，3.3 分)
下列选项中,幂运算的符号为( )。A  
A.**
B.*
C.++
D.%

>16.(单选题，3.3 分)
以下关于字符串.strip()方法功能说明正确的是:D  
A.替换字符串中特定字符
B.按照指定字符分割字符串为数组
C.连接两个字符串序列
D.去掉字符串两侧指定字符

>17.(单选题，3.3 分)
哪个选项是下面代码的执行结果?B  
name="Python语言程序设计课程"  
print(name[0],name[2:-2],name[-1])  
A.P thon语言程序设计 课
B.P thon语言程序设计 程
C.P thon语言程序设计课 课
D.P thon语言程序设计课 程
